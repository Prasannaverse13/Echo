{
  "name": "echo-browser-executor",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright-backed headless browser that the Echo ADK agent controls over HTTP. Lets the agent open a real browser, click, type, and extract data — i.e. actually do the workflows the user recorded.",
  "main": "index.ts",
  "scripts": {
    "start": "node --import tsx index.ts",
    "dev": "node --import tsx --watch index.ts"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.11.0",
    "express": "^4.21.2",
    "playwright": "^1.49.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
