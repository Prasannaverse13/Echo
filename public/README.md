# Media assets

This folder ships only assets that are part of the app's source-of-truth
(architecture diagram). Binary media (hero video, ambient audio) is not
committed — add the file here and reference it from
`src/components/ui/echo-hero.tsx` if you want it on the landing page.

- **`architecture.png`** — rendered in the README and in
  `docs/architecture.md`.

